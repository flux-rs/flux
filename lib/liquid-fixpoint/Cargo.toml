[package]
edition.workspace = true
name = "liquid-fixpoint"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
derive-where = "1.2.7"
itertools = "0.14.0"
serde = "1.0"
serde_json = { version = "1.0.140", features = ["std", "alloc", "arbitrary_precision"] }

[features]
nightly = []

[lints]
workspace = true
