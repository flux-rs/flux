import ImplTrait.ImplTrait00.Flux.Prelude
open Classical
set_option linter.unusedVariables false


namespace F



def TestClient := 
 ∀ (it₀ : Int),
  ∀ (a'₁ : Int),
   ∀ (a'₂ : Int),
    (1 ≤ a'₂) ->
     (0 ≤ a'₂)
end F
