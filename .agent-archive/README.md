# Archived agent docs

These are narrative notes written by prior coding-agent sessions. **They are not the current plan**
and may contain incorrect claims (in particular, conflation of Haskell's `Solver/Eliminate.hs`
FUSION path with `Horn/Transformations.hs`'s syntactic rewrite, and unvalidated assertions about
Flux's existing behavior).

The authoritative plan lives at the repo root: `NONCUT_PLAN.md`.

This directory may be deleted at the user's discretion.
