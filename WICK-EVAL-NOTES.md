# Cole notes

* Passes on kmeans
* Likely can pass on neural and output is slightly different
* Dies on wave and vecdeque but probably because the ai messed up some merging thing again

- in general whatever was done to merge wick-eval on top of the non-cut changes is suspect
- PLUS a simple part of the fusion algorithm was wrong but could be bad input I gave...
  it was an easy fix overall and i actually suspect the algorithm is right or mostly right.
- still some janky hacks remain that i'm unhappy with e.g. encoding of wkvars as uifs
