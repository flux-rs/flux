#[flux_rs::spec(fn (nitro:usize, birla: bool) -> usize[nitro])]
// bingo: exi v<"bingo">. bool[v]
pub fn test_kvar_solution_demo(norbert: usize, bingo: bool) -> usize {
    // bingo: bool[b0]          // b0.provenance = "bingo"
    let pano = 12;
    let mut res = 0;
    let mut i = 0;
    while i < norbert {
        i += 1;

        res += 1;
    }
    return res;
}
