fn abs(n0: {int | true}; n: own(n0)) ret k(r: {int | V >= 0}; own(r)) =
  let b = alloc(1);
  letcont b0(n2: {int | _ }; n: own(n2);) =
    jump k(n)
  in
  b := n < 0;
  if b then
    n := 0 - n;
    jump b0()
  else
    jump b0()
